function res = steamToLiquid(steamTemp, liquidTemp, steamMass, liquidMass)
        
        steamSA = (lengthBar + 2 * steamThickness) * (pi * (diamaterBar + 2 * steamThickness)) + 2 * pi * (diamaterBar/2 + steamThickness)^2;
    end